name: CI

on:
  pull_request:
  workflow_dispatch:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: pnpm/action-setup@v6

      - uses: actions/setup-node@v6
        with:
          node-version-file: '.nvmrc'
          cache: pnpm

      - run: pnpm install --frozen-lockfile

      - run: pnpm lint

      - run: pnpm typecheck

      - run: pnpm test

      - run: pnpm build

      # axe-core run against the SSG build via Playwright. See
      # docs/a11y-audit-2026-05.md for scope and manual follow-ups.
      - name: Cache Playwright browsers
        uses: actions/cache@v5
        with:
          path: ~/.cache/ms-playwright
          key: playwright-${{ runner.os }}-${{ hashFiles('pnpm-lock.yaml') }}

      - name: Install Playwright browsers
        # No --with-deps: ubuntu-latest already ships Chromium's system libs, and
        # the apt-get step it triggers hangs on the current runner image (verify
        # has hung here on every run since the Node 24 / actions bump in #63).
        run: pnpm exec playwright install chromium

      - run: pnpm test:a11y
